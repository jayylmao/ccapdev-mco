const User = require('../models/user-model.js');
const Post = require('../models/post-model.js');

const renderSearchPage = async (req, res) => {
    try {
        
    } catch (error) {
        console.error(error);
    }
};

modules.export = { renderSearchPage };